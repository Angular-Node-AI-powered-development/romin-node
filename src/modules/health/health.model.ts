/**
 * Health module model. No DB schema for health check; placeholder for pattern.
 * DB-backed modules would define queries/schema here.
 */
export const schema = {
  name: 'health',
  description: 'Health check endpoint; no persistent data',
} as const;
